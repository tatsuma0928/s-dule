%h1 Listing events

%table
  %thead
    %tr
      %th Title
      %th Body
      %th Start date
      %th End date
      %th
      %th
      %th

  %tbody
    - @events.each do |event|
      %tr
        %td= event.title
        %td= event.body
        %td= event.start_date
        %td= event.end_date
        %td= link_to 'Show', event
        %td= link_to 'Edit', edit_event_path(event)
        %td= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Event', new_event_path


.events-head
  .events-head__title 予定
  .events-head__body 内容 
  .events-head__start-date 開始日時
  .events-head__end-date 終了日時
  .events-head__show-btn
  .events-head__edit-btn
  .events-head__destroy-btn

.events-head
  .events-head__title 予定
  .events-head__body 内容 
  .events-head__start-date 開始日時
  .events-head__end-date 終了日時
  .events-head__show-btn
  .events-head__edit-btn
  .events-head__destroy-btn
  

  